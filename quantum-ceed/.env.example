# Copy this file to `.env` to enable the real Voice AI Coach.
# Without this, the Coach screen falls back to scripted replies + on-device TTS.
#
# SECURITY: this key is bundled into the app binary. Use a server proxy
# before shipping to production.

EXPO_PUBLIC_OPENAI_API_KEY=
